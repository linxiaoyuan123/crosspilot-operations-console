{
  "name": "supportops-console",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency-free implementation, support and operations console built with Node.js and SQLite.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/db.js && node --check src/server.js && node --check public/app.js"
  },
  "keywords": [
    "it-support",
    "implementation-engineer",
    "technical-support",
    "sqlite",
    "operations"
  ],
  "license": "MIT"
}
