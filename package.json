{
  "name": "crosspilot-operations-console",
  "version": "3.0.0",
  "private": true,
  "description": "CrossPilot cross-border marketplace operations decision console.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/store.js && node --check src/metrics.js && node --check src/imports.js && node --check src/reports.js && node --check src/server.js && node --check public/app.js"
  },
  "keywords": [
    "cross-border-ecommerce",
    "amazon-operations",
    "marketplace-analytics",
    "listing-optimization",
    "advertising",
    "inventory",
    "operations"
  ],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
